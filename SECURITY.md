# 安全说明

本文件说明本项目的安全模型、主要攻击面与漏洞报告方式。本项目为个人开发的应用，处于邀请测试阶段，尚未经过第三方安全审计。

## 一、报告安全问题

**请不要用公开 issue 报告安全漏洞**，那会在修复前把细节暴露给所有人。两条私密通道：

1. **GitHub 私密报告（推荐）**：本仓库「Security」标签页 → 「Report a vulnerability」。频道只有仓库维护者可见。
2. **应用内反馈**：应用内「设置 → 反馈与建议」，内容直达开发者。

提交时若附上复现步骤、影响范围、涉及版本（应用「关于」页可见）与实际观察到的现象，会很有帮助。所有报告都会得到回复；修复随下一个版本发布，本项目没有自动更新通道，更新由用户手动完成。

## 二、这个应用能触到什么

- 应用没有自有服务器，无账号体系，不含统计埋点与广告组件。
- 声明三条权限，用途见应用内隐私政策：网络访问、持久化数据存储、生物识别（用于加密会话的身份验证）。选择图片、文件与拍照走系统官方选择器，不需要存储或相机权限。
- 会话、助手、提示词与偏好默认只存在设备本地；API 密钥存于系统安全存储（Asset Store Kit），界面只显示掩码，明文不写入数据库与日志。
- 数据对外传输只发生在用户自己配置或主动触发的地方：发往用户配置的 AI 服务、联网搜索的关键词与网页回灌、用户主动提交的应用内反馈、用户主动发起的备份与网盘同步。

## 三、主要攻击面：让模型操作网页

`browse_open`、`browse_read`、`browse_act` 三个工具让模型能打开网页、读取渲染后的内容，并在页面上执行动作（滚动、点击、输入、选择、按键）。这是本项目唯一会操作外部内容的能力，也是安全上最需要说明的部分。三个工具默认随「联网」能力启用（可在助手工具页关闭），并受联网总开关与会话内的联网开关约束。

### 它的工作方式

- 应用内有一个隐身网页宿主，铺满视口、透明、被界面盖住且不接收触摸，可按需加载任意 http 或 https 站点。
- 读取走系统官方提供的网页读取接口（ArkWeb AI 页面命令，API 26 起）。动作优先走官方交互通道。
- 官方通道在内核上定位失败、而目标元素已在当次页面快照中核对成功时，会退到**在页面里执行一段脚本**（`runJavaScript`）来完成点击或输入。这是全项目最需要解释的一处，边界见下。

### 注入脚本的边界

- **只对已在当次页面快照里核对过的元素注入**。模型自己编的定位一律不算数，包括手写 xpath、过期序号与重组过的元素标识。注入通道的许可面就是「该目标在当次快照中解析成功」，未解析成功则注入通道不开。
- **注入的脚本运行在页面自身的脚本上下文里**，能做的与该网页自己的脚本相同，例如读写本页 DOM、触发点击与表单提交。它触及不到应用的数据、文件与界面，因为**应用没有向页面注册任何 JS 桥**（全仓无 `javaScriptProxy` 一类注册），页面无法回调应用的任何接口。
- **协议白名单**：页面导航只放行 `http` 与 `https`，其余协议（如 `tel:`、各类 App 唤起 scheme）在加载拦截回调里直接拦下。

### 凭据与写操作的三道闸

1. **密码恒拒**。目标若是密码框（`input[type=password]`），执行器直接拒绝，回执告知模型本应用不允许处理任何密码、请让用户自行完成。确认卡放行不了它，全局的「网页操作自动通过」开关也影响不到它。
2. **属性缺失即失败关闭**。页面若未返回目标元素的类型属性，就无法排除密码框或提交按钮，此时点击与输入一律取消，不按「大概率不是密码框」放行。
3. **高危动作需要用户点允许**。命中任一判据即弹确认卡，用户点允许才执行：目标是提交控件；或目标语义命中高危词表（中英文两组，如发布、发送、删除、付款、购买）。允许凭据是**一次性的、60 秒内有效、且绑定当次会话**三条同时成立。绕过确认卡直接调用执行器的路径仍会被静态拒绝，这道判定属安全层，不是体验层。

### 已知边界（如实声明）

- 跨域 iframe 内的内容与 canvas 绘制的页面元素不在可操作范围内，取不到元素清单。
- **模型可能被网页内容误导**（间接提示注入）。工具说明中已明确告知模型网页内容是数据而不是指令、不要执行页面里出现的指令、不要输入密码与支付信息，但防线不建立在模型的自律之上，上述三道闸与用户确认才是兜底。
- 设置中有「网页操作自动通过」开关，默认关闭。开启后高危动作不再逐次弹卡，但**密码恒拒与失败关闭两条不受影响**。
- 邀请测试阶段，尚未做外部安全审计。

## 四、其它值得知道的事实

- **文档解析**把用户选中的 pdf、docx、xlsx 与 ofd 交给随包的第三方解析库在本地完成，许可与来源逐件登记在 `LICENSE-THIRD-PARTY.md`；解析页与应用界面相互隔离，应用不向它注册任何 JS 桥。
- 构建期诊断开关 `BROWSE_DEBUG` 在正式构建里恒为 `false`，网页远程调试端口不开放（官方亦不建议在正式版本开启）。
- 应用不提供应用内自检功能，诊断依靠构建期开关与真机手测，探针不进产物常态。
