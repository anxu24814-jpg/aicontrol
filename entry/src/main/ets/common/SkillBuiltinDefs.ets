// 内置技能定义单源（2026-09-19 内置技能首批笔新建）：全部内置技能的固定 id、名称、触发
// 描述、图标、正文与「是否默认启用」以 SKILL_BUILTIN_DEFS 数组为唯一来源，元技能「创建技能」
// 的正文拼接自 SkillDb.ets 迁入（原样搬迁，三处上限数字仍随 SkillText 常量拼接防漂移）。
// 消费方 = SkillDb.ensureBuiltinSkills（逐条幂等插入 + 代数标记升代迁移）；本文件纯数据，
// 只允许 import SkillText 上限常量，禁引 DB/AppStorage（断言脚本剥 import 后直跑本文件真值，
// assert-app-domain 130 组）。
// · 升代机制（首版「只认 id 不认内容」登记二期的兑现）：SKILL_BUILTIN_GENERATION 每次修订
//   内置内容时 +1 并同步改 SKILL_BUILTIN_DEFS 常量，SkillDb 迁移面在启动时把已装用户的
//   内置行按定义刷新（只升不降，标记语义见 SkillDb seed 节注释）——修订正文必须动两处：
//   定义常量 + 代数 +1，漏一代数 = 已装用户拿不到新内容。
// · autoEnable = 首插时并入默认助手启用集（首启即用——内置技能不启用则清单不注入、
//   use_skill 不声明，对模型完全不可见；已存在的行不重并，恢复备份不翻案用户关闭态——
//   并入判据 = SKILL_PENDING_JOIN_KEY 登记（只由 seed 新插路径写入），见
//   SkillDb.flushPendingAutoEnable 注释）；恒 true 的 = 三内容件 + 知识整理（都是「用户开口
//   就走流程」的活儿），恒 false 的只有「创建技能」一件（沿用其「引导用户手动开」语义）。
// · 新增内置件四步：defs 数组加条目（id 形如 skill-builtin-xxx 全小写连字符）→ autoEnable
//   拍板 → 断言 130 组锚点补该件 → SKILL_BUILTIN_GENERATION +1。
import { SKILL_CONTENT_MAX, SKILL_DESC_MAX, SKILL_NAME_MAX } from './SkillText';

// 内置 seed 代数标记键（AppStorage + PersistentStorage，EntryAbility 注册；值 = 字符串数字，
// '' = 从未迁移。本机安装态标记，AppPreferences BACKUP_EXCLUDED_PREF_KEYS 豁免随备份走）
export const SKILL_BUILTIN_SEEDED_KEY: string = 'skill_builtin_seeded';

// 待并入登记键（2026-09-20 D5-3 修复；AppStorage + PersistentStorage，EntryAbility 注册，
// 缺省 '[]'）：JSON 字符串数组 = 「本机 seed 插入、自动并入尚未落定」的内置 id。与标记键
// 同为安装态标记（同入 BACKUP_EXCLUDED_PREF_KEYS）——首启迁移超 SKILL_ENABLED_MAX 整组
// 跳过、或迁移中途抛错时凭它下次启动补跑；旧实现按「本轮新插」这一易失态判定并入，两窗口
// 都会永久丢失「默认助手开箱启用」。并入成功才移除（消费见 SkillDb.flushPendingAutoEnable）
export const SKILL_PENDING_JOIN_KEY: string = 'skill_builtin_pending';

// 当前内置定义代数：修订任何内置内容 / 新增内置件时 +1（迁移触发阈，见头注释升代机制）
// 2 = 2026-09-22 知识整理笔（新增 skill-builtin-knowledge-curate）
export const SKILL_BUILTIN_GENERATION: number = 2;

export interface SkillBuiltinDef {
  id: string;
  name: string;
  description: string;
  icon: string;
  content: string;
  autoEnable: boolean;
}

export const SKILL_BUILTIN_DEFS: SkillBuiltinDef[] = [
  {
    id: 'skill-builtin-plan-interview',
    name: '方案访谈',
    description: '当用户想把一个想法或需求变成可执行的方案时使用。先访谈澄清再给带取舍的候选，拍板后出完整方案。',
    icon: 'person',
    autoEnable: true,
    content: [
      '你在帮用户把一个想法打磨成可执行的方案。本技能的核心纪律是先访谈后方案：不拿到一句话需求就直接输出完整方案，也不挤牙膏式地一次只问一个问题拖上好几轮。',
      '',
      '第一步 判断需求成熟度',
      '用户开口后先分诊：',
      '· 已带着完整背景与明确要求的，跳过访谈直接进入第三步给候选，只补问真正缺失的关键项。',
      '· 只有一句模糊想法的，走完整访谈流程。',
      '',
      '第二步 一轮问齐',
      '用 ask_user 工具把关键问题一次性问完。问题覆盖四个面：',
      '· 目标，做成什么样算成功，给谁用，解决什么问题。',
      '· 约束，时间、预算、技术选型、平台、不能动的东西。',
      '· 现状，已经有什么，试过什么，卡在哪里。',
      '· 偏好，用户心里已有的倾向，包括没说出口的。',
      '一次提问以三到五个为限，优先问「答案会改变方案走向」的问题。例如要做「一个记录饮食的小工具」，值得问的是「手记还是拍照为主」「要不要统计热量」这类分叉点，而不是可以用默认值兜底的问题。',
      '用户答不全就按已答部分推进，未答项在方案里列为显式假设并写明影响，不要为凑齐答案反复追问。',
      '',
      '第三步 给候选，不只给一条',
      '基于澄清结果给出二到三个方向不同的候选方案。每个候选写清四件事：',
      '· 核心思路，两三句话说清怎么做。',
      '· 代价，要投入的时间、金钱、精力或放弃的东西。',
      '· 风险，最可能翻车的点。',
      '· 适用场景，什么情况下选它而不是别的。',
      '候选之间要有真实差异，例如自建与现成方案、轻量与完整、快糙与稳妥，不要把同一个方案换三种说法重讲一遍。可以给推荐并说明理由，但把选择权交给用户，不替用户拍板。',
      '',
      '第四步 出完整方案',
      '用户选定后输出完整方案，结构固定四段：',
      '1. 结论先行，一段话说清最终做什么、为什么是它。',
      '2. 分步落地，按执行顺序列步骤，每步给出可自查的完成标志。',
      '3. 代价与风险，如实写不粉饰，已知的未知也写明。',
      '4. 明确不做项，列出这次不做的事，防止范围蔓延。',
      '方案中的技术名词、产品名、参数名保持原文，不擅自翻译或改写。',
      '',
      '第五步 收尾',
      '方案交付后问一句是否要调整。若用户表示这类讨论不想留档，如实告知可以在会话页右上更多菜单里选「隐私会话」重新开聊，隐私会话不落库、退出即不再保留。',
      '',
      '边界与降级',
      '· ask_user 不可用（被关闭或当前模型不支持工具调用）时，降级为在回复正文里一次性列出问题请用户作答，纪律不变。',
      '· 用户中途改需求，回到第三步重给候选；已澄清的事实继续使用，不推翻重来。',
      '· 用户要的是某个具体步骤的执行细节或代码实现时，如实告知这超出了方案访谈的范围，然后按其原始需求直接作答。',
      '· 一次会话服务一个方案主题，用户换了主题就当作新方案重新走流程。'
    ].join('\n')
  },
  {
    id: 'skill-builtin-writing-polish',
    name: '写作精修',
    description: '当用户请润色、改写或诊断一段文字时使用。先给诊断再给对照稿，保留作者本意。',
    icon: 'text_alignleft',
    autoEnable: true,
    content: [
      '你在帮用户改稿。本技能的纪律是先诊断后动笔：没读出具体问题就整段重写，是改稿最大的失职。',
      '',
      '第一步 定位文体与读者',
      '先判断这是什么文体、写给谁看、要达到什么效果。文体决定标准，同一段文字在周报与朋友圈里的问题不是同一种问题。三者判断不了就问一句，一次把空补齐；用户已经说明的不要重复问。',
      '',
      '第二步 诊断',
      '把原文完整读一遍再下第一条诊断。对照原文列出三到五条具体问题，每条引用原句说话，按影响从大到小排：',
      '· 结构，段落顺序、信息密度、重点位置。',
      '· 语气，与文体和读者是否匹配，有没有用力过猛或过于随意。',
      '· 节奏，长句连排、一段到底、该断不断。',
      '· 冗余，重复表述、空话套话、可删的修饰。',
      '· 准确，事实矛盾、术语误用、指代不清。',
      '诊断要写成「哪一句有什么问题」，不写「整体还可以更好」这类无法执行的评语。',
      '',
      '第三步 对照稿',
      '按诊断逐条给出修改，展示形态为原句与改句并列，每处用一句话点明改了什么；零散小改合并说明，不为每处微调单开一段。改不动的句子宁可保留原样并说明原因，不为改而改。改动克制四条：',
      '· 保留作者的本意、立场与个人风格，改表达不改观点。',
      '· 技术内容保持原文，URL、协议名、变量名、参数名、引文一律不动。',
      '· 篇幅保持在原文量级，不擅自扩写或压缩，用户明确要求扩缩除外。',
      '· 拿不准作者意图的地方给出两种改法并说明各自假设，让用户挑。',
      '',
      '第四步 全文交付',
      '需要整篇交付时，先给全文修改稿，再附修改要点清单，与第二步的诊断逐条对应；诊断里有条目没改的，说明原因。',
      '',
      '边界',
      '· 这是精修技能：用户要的是从头写一篇新文章时，不套用本流程，直接按需求写作。',
      '· 不上写作课：不讲写作理论，不评价用户水平，建议只落在具体句子上。',
      '· 用户只要结果时，先给改后全文，诊断收进末尾一小节。',
      '· 涉及事实的部分只指出可疑之处并说明为何可疑，不替用户断定真伪。'
    ].join('\n')
  },
  {
    id: 'skill-builtin-brainstorm',
    name: '放开想',
    description: '当用户要灵感、点子或思路发散时使用。数量先行再分档收拢，选中后深入展开。',
    icon: 'lightbulb',
    autoEnable: true,
    content: [
      '你在帮用户发散思路。本技能的纪律是数量先行：先不管好坏把量堆起来，再收拢筛选。过早挑三拣四是发散最大的敌人。',
      '',
      '第一步 数量先行',
      '第一轮一口气给出至少十个方向，每个方向一行短句，不展开、不论证、不自我审查。点子允许粗糙、允许互相矛盾，矛盾恰恰是方向的信号。此时判劣标准只有两条：与话题完全无关的不要，明显冒犯的不要，其余一律先写下来。禁止「赋能」「生态」「闭环」这类空转的套话，每个点子要么给一个具体做法，要么给一个具体画面。',
      '',
      '第二步 四类联想扩展',
      '第一轮数量不足或明显同质时，用四类联想强制扩展：',
      '· 类比，别的领域怎么解决过类似问题。',
      '· 替代，把其中的资源、渠道、形式换掉会怎样。',
      '· 组合，两个点子各取一半拼起来会怎样。',
      '· 反转，把前提倒过来还成立吗。',
      '每类至少产出两个新方向。',
      '',
      '第三步 分档收拢',
      '把全部点子分成三档，各配一句理由，理由写「为什么归这档」，不写「好不好」：',
      '· 贴题可做，直接对准需求，现有条件就能启动。',
      '· 有趣但偏，与需求有关联，但需要改造或等条件。',
      '· 发散跳板，本身不解决问题，但能引出下一个想法。',
      '档内按与需求的相关度排序。收拢时不删除任何点子，分档本身就是筛选。',
      '',
      '第四步 深入',
      '用户挑中某个方向后，把它展开成可执行的起点：第一步具体做什么、需要准备什么、最容易死在哪。展开不超过一屏，用户要继续再往深走。展开时如实评估，不为迁就用户的选择而美化。',
      '',
      '边界',
      '· 用户只要一个答案、时间紧急或话题严肃时，不套用本技能，直接作答。',
      '· 本技能不做可行性深评与完整方案；用户选定方向要往下走时，说明可以继续深入，或另开方案讨论。',
      '· 用户否定某个点子时，记住否的原因用于调整后续方向，不反复推销被否的点子。',
      '· 全程短句，不写排比抒情，不堆形容词。'
    ].join('\n')
  },
  {
    id: 'skill-builtin-knowledge-curate',
    name: '知识整理',
    description: '当用户想把聊过的内容或某段资料整理成知识库条目时使用。先问清落点与粒度，复述草稿，用户明确要求后再写入。',
    icon: 'book',
    autoEnable: true,
    content: [
      '你在帮用户把内容整理成知识库条目。本技能的核心纪律是三步：先问清落点，再复述草稿，用户明确要求后才写入。落点不清就往库里写，写进去的是废条目；用户没点头就写，越过了授权边界。',
      '',
      '第一步 判断材料',
      '只整理当前对话里已经出现的内容。用户说「之前聊过的」，而那段内容不在本对话里时，请他贴过来或说清是哪一段再开始，不要凭印象替他回忆，也不要检索历史会话。材料撑不起一个条目时，先问清缺的那部分，或者如实说明现在整理不了。',
      '',
      '第二步 问清落点',
      '用 ask_user 把关键问题一次问完，一次不超过三个，只问会改变落点的问题：',
      '· 落到哪本书。先调用 manage_knowledge 的 list（不带 book 参数）看现有知识库，有合适的就报出书名让用户挑，没有就建议新建一本。',
      '· 条目粒度。问清是一条装一个知识点，还是一条装一整段。内容多时说明你打算拆成几条，让他确认条数与分法。',
      '· 关键词要不要指定。关键词影响日后能不能被搜到，用户给得出就用他的，给不出由你从正文里提，不要为这件事单独追问一轮。',
      '用户答不全就按已答部分推进，未答项用默认值处理，并在复述草稿时说明你是怎么定的。',
      '',
      '第三步 起草并复述',
      '把每一条写成三件：',
      '· 条目名，一眼可辨的短名，例如 TCP 三次握手。',
      '· 关键词，多个用逗号分隔，取正文里最可能被问到的词。',
      '· 正文，纯文本原文，写清含义、规则或背景。',
      '正文必须自包含，不出现「这个」「刚才」「上面说的」这类指回本次对话的词。它日后会被单独读到，换一个对话单独看也要读得懂。用户没有确认的推断不要写进正文，事实有疑点就先问一句。',
      '起草完把条目名、关键词、正文原文贴给用户过目，他改了就按改后的写。',
      '',
      '第四步 写入',
      '只在用户明确要求写入后再调用 manage_knowledge：',
      '· 新建库用 create_book，书名一次定好，遇到同名会被拒绝，换个名字再试。',
      '· 写条目用 create，book 与 title 逐字取自 list 回执，不要凭记忆拼写。',
      '· 一次调用写一条，多条就逐条来。',
      '写入成功后如实复述改了什么。新建的知识库默认不被任何助手启用，检索不到，记得提醒用户到助手编辑页的知识库里启用它。',
      '',
      '边界与降级',
      '· manage_knowledge 不可用（本助手工具页的「知识库」卡没开）时，如实告知工具当前用不了并引导用户去开启，同时把起草好的条目原文贴在回复里让他先收着，不要声称已经写入。',
      '· 条目名在同一个库内不允许重名，工具拒绝时按回执提示换名，或改为更新已有条目。',
      '· 回执说无法确定改动哪一条（存量重名）时，如实转达并指引用户到设置的知识库页处理，不要自行猜测。',
      '· 条目名、关键词与正文都有长度上限，超限会被拒绝并提示精简。不要猜上限，也不要为了塞进去把内容砍得读不懂。',
      '· 不写入口令、密钥等敏感信息。',
      '· 关于用户本人的偏好、背景与交代过的事，属于记忆库；可复用的做法与流程，属于技能库，交给「创建技能」那件；本技能只管与用户本人无关的领域知识条目。',
      '· 用户只是要一段整理好的文本、并不想入库时，直接给文本，不要写库。'
    ].join('\n')
  },
  {
    id: 'skill-builtin-create',
    name: '创建技能',
    description: '当用户想把某个做法、流程、规范或偏好沉淀成可复用' +
      '技能，或询问怎么创建技能时使用。引导补齐意图，起草名称、触发描述与正文，经创建工具保存' +
      '入库，并告知如何启用。',
    icon: 'wand_and_stars',
    autoEnable: false,
    content: [
      '你在帮用户把一个做法沉淀成可复用技能。技能由三件组成：',
      '· 名称，简短中文名，像「周报撰写」「文献速读」一样一眼可辨，' + SKILL_NAME_MAX + ' 字内。',
      '· 触发描述，一句祈使句写清什么时候该用，例如「当用户要写每周工作汇总时使用」。写用户' +
        '意图与适用场景，不写实现细节；场景多就都列上，宁全勿缺，' + SKILL_DESC_MAX + ' 字内。',
      '· 正文，给模型照做的步骤说明书，按步骤写清做法，可以指挥现有工具（工具决定能不能做，' +
        '技能决定怎么做），' + SKILL_CONTENT_MAX + ' 字内。',
      '工作方式：',
      '1. 用户意图已经清楚时直接起草三件；缺什么就用一句话问什么，不要连环追问。',
      '2. 起草后向用户复述三件要点，确认后调用 manage_skills 工具保存。',
      '3. 调用失败返回同名清单时，换个名称与用户确认后再试；返回库满时建议用户先清理不再需要' +
        '的技能。',
      '4. 保存成功后告知用户，技能已入库但还未被任何助手启用，可在助手编辑页的技能分区开启；' +
        '若本助手工具页「技能管理」卡未开导致工具不可用，引导用户先打开再试。',
      '正文不要写入口令、密钥等敏感信息。一次只创建一件技能。'
    ].join('\n')
  }
];
